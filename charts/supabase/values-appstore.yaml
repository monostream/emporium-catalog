supabase:
  publicUrl: api.{{ .AppStore.Subdomain }}.{{ .AppStore.DNSZone }}

  auth:
    image:
      repository: supabase/gotrue
      tag: v2.70.0

  studio:
    ingress:
      enabled: true

      pathType: Prefix

      annotations:
        glass.monostream.com/enabled: "true"
        kubernetes.io/tls-acme: "true"

      hostname: {{ .AppStore.Subdomain }}.{{ .AppStore.DNSZone }}
      tls: true

  kong:
    ingress:
      enabled: true
      hostname: api.{{ .AppStore.Subdomain }}.{{ .AppStore.DNSZone }}
      tls: true

      annotations:
        glass.monostream.com/enabled: "true"
        kubernetes.io/tls-acme: "true"
